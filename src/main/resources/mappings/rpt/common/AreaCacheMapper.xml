<?xml version="1.0" encoding="UTF-8" ?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" "http://mybatis.org/dtd/mybatis-3-mapper.dtd">

<mapper namespace="com.kkl.kklplus.provider.rpt.common.mapper.AreaCacheMapper">
    
    <select id="getAllTownList" resultType="com.kkl.kklplus.entity.rpt.web.RPTArea">
        SELECT
              a.id,
              a.parent_id   AS "parent.id",
              a.name
        FROM  rpt_sys_area a
        WHERE a.type = 5 AND a.del_flag = 0
        LIMIT 200000
    </select>

    <select id="getAllCountyList" resultType="com.kkl.kklplus.entity.rpt.web.RPTArea">
        SELECT
              a.id,
              a.parent_id   AS "parent.id",
              a.parent_ids,
              a.full_name,
              a.name
        FROM  rpt_sys_area a
        WHERE a.type = 4 AND a.del_flag = 0
        LIMIT 10000
    </select>


    <select id="getAllCityList" resultType="com.kkl.kklplus.entity.rpt.web.RPTArea">
        SELECT
              a.id,
              a.parent_id   AS "parent.id",
              a.parent_ids,
              a.full_name,
              a.name
        FROM  rpt_sys_area a
        WHERE a.type = 3 AND a.del_flag = 0
        LIMIT 10000
    </select>



    <select id="getAllProvinceList" resultType="com.kkl.kklplus.entity.rpt.web.RPTArea">
        SELECT
              a.id,
              a.parent_id   AS "parent.id",
              a.parent_ids,
              a.full_name,
              a.name
        FROM  rpt_sys_area a
        WHERE a.type = 2 AND a.del_flag = 0
        LIMIT 10000
    </select>

</mapper>