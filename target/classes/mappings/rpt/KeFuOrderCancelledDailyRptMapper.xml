<?xml version="1.0" encoding="UTF-8" ?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" "http://mybatis.org/dtd/mybatis-3-mapper.dtd">

<mapper namespace="com.kkl.kklplus.provider.rpt.mapper.KeFuOrderCancelledDailyRptMapper">
    <select id="getKeFuCancelledOrderList" resultType="com.kkl.kklplus.entity.rpt.RPTKeFuOrderCancelledDailyEntity">
        SELECT
        kefu_id AS "keFuId",
        day_index AS "dayIndex",
        count(id) AS orderSum
        FROM
        rpt_cancelled_order_daily
        <where>
            system_id = #{systemId}
            AND order_close_date >= #{startDate}
            AND <![CDATA[ order_close_date <= #{endDate}]]>
            <if test="keFuId != null and keFuId != 0">
                and kefu_id = #{keFuId}
            </if>
            <if test="productCategoryIds.size() != 0">
                <choose>
                    <when test="productCategoryIds.size() == 1">
                        and product_category_id = #{productCategoryIds[0]}
                    </when>
                    <otherwise>
                        and product_category_id in
                        <foreach item="productCategoryIds" index="index" collection="productCategoryIds" open="("
                                 separator="," close=")">
                            #{productCategoryIds}
                        </foreach>
                    </otherwise>
                </choose>
            </if>
            <if test="quarter != null and quarter != ''.toString()">
                and quarter = #{quarter}
            </if>
        </where>
        group by keFuId,dayIndex
        LIMIT 8000
    </select>


    <select id="getKeFuOrderCancelledMonth" resultType="java.lang.Long">
        SELECT
        count(0)
        FROM
        rpt_cancelled_order_daily
        <where>
            system_id = #{systemId}
            AND order_close_date >= #{startDate}
            AND <![CDATA[ order_close_date <= #{endDate}]]>
            <if test="keFuIds.size() != 0">
                <choose>
                    <when test="keFuIds.size() == 1">
                        and kefu_id  = #{keFuIds[0]}
                    </when>
                    <otherwise>
                        and kefu_id  in
                        <foreach item="keFuIds" index="index" collection="keFuIds" open="("
                                 separator="," close=")">
                            #{keFuIds}
                        </foreach>
                    </otherwise>
                </choose>
            </if>
            <if test="productCategoryIds.size() != 0">
                <choose>
                    <when test="productCategoryIds.size() == 1">
                        and product_category_id = #{productCategoryIds[0]}
                    </when>
                    <otherwise>
                        and product_category_id in
                        <foreach item="productCategoryIds" index="index" collection="productCategoryIds" open="("
                                 separator="," close=")">
                            #{productCategoryIds}
                        </foreach>
                    </otherwise>
                </choose>
            </if>
            <if test="quarter != null and quarter != ''.toString()">
                and quarter = #{quarter}
            </if>
        </where>
    </select>

    <select id="hasReportData" resultType="java.lang.Integer">
        SELECT
        count(0)
        FROM
        rpt_cancelled_order_daily
        <where>
            system_id = #{systemId}
            AND order_close_date >= #{startDate}
            AND <![CDATA[ order_close_date <= #{endDate}]]>
            <if test="keFuIds.size() != 0">
                <choose>
                    <when test="keFuIds.size() == 1">
                        and kefu_id  = #{keFuIds[0]}
                    </when>
                    <otherwise>
                        and kefu_id  in
                        <foreach item="keFuIds" index="index" collection="keFuIds" open="("
                                 separator="," close=")">
                            #{keFuIds}
                        </foreach>
                    </otherwise>
                </choose>
            </if>
            <if test="productCategoryIds.size() != 0">
                <choose>
                    <when test="productCategoryIds.size() == 1">
                        and product_category_id = #{productCategoryIds[0]}
                    </when>
                    <otherwise>
                        and product_category_id in
                        <foreach item="productCategoryIds" index="index" collection="productCategoryIds" open="("
                                 separator="," close=")">
                            #{productCategoryIds}
                        </foreach>
                    </otherwise>
                </choose>
            </if>
            <if test="quarter != null and quarter != ''.toString()">
                and quarter = #{quarter}
            </if>
        </where>
    </select>
</mapper>